// Basic spellcheck stub: no correction suggestions for now.

export function buildSpellcheckIndex(tokenFrequency) {
  return {};
}

export function suggestQueryCorrection(query, spellcheckIndex, tokenFrequency) {
  return null; // no suggestions
}